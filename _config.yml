title: "Positioning"
description: "Modular wisdom system for a life of signal, strength, and structured presence."
url: "https://www.pavlo.uk"
baseurl: "" # keep blank for root deployment

markdown: kramdown
sass:
  style: compressed

collections:
  pages:
    output: true
    permalink: /:path/

# keep your existing settings above…
exclude:
  - _pages/
  - archive/
  - node_modules/

theme: minima
